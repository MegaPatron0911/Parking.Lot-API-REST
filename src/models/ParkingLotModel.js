// Aquí se definirán las clases de modelo y lógica de negocio
// Ejemplo de clase base (aún sin mapeo a la base de datos)

class ParkingLotModel {
  constructor() {
    // Inicialización de propiedades
  }

  // Métodos de lógica de negocio
}

module.exports = ParkingLotModel;
